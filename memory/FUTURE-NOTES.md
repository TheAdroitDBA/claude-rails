# Future Feature Notes

One-line scope reminders for features in the queue. Captured here so /clear does not lose them.

## Feature D (`/diag`)

- Add an aggregate **confidence score** dimension: rolls discovery cost + criteria PASS rate + hook health + doc staleness into a single number per repo. Surfaces drift between "what the framework thinks is healthy" and "what an operator would call healthy." Lock scope when /n'd.
